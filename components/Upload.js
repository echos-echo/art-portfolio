// this will be the component where you are able to upload more images
// should be password access only (no need to encrypt; personal use only)
// a form that takes in
// - image file (jpg, jpeg, png)
// - image type (sketch, comic, illustration, etc)
// - image tags

export default function Upload() {
    return (<></>);
}