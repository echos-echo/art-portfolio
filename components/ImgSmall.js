// this will be the component that shows thumbnail of a given image
// may take in props in the future:
// - file/url of the image
// - image details (likely as an object)

export default function ImgSmall() {
    return (<></>);
}